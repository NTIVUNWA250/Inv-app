# Copy this file to `.env.local` and fill in real values.
# The web app talks only to the shared inventory-api (supabase/api), never to
# Supabase directly. Point this at wherever that API is running.

API_URL=http://localhost:4000
